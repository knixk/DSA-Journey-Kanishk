class Solution {
public:
    int maxArea(vector<int>& height) {
        
        // find the max from the left side

        // find the max from the right side?


        // diff between them

        // multiply the number of times seen


    // my problem is taking input...


    // not solvin questions..

    // then take the essential course or rather take that course...

    // or if u wanna od take the js course i mean

    // it's better u do it in 

    // cuz u have to do it either way

    // sop this is good
    }
};